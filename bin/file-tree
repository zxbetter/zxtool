#!/usr/bin/env node

/**
 * 打印目录树
 */

const program = require('commander')
const type = require('../src/util/type')
const fileTree = require('../src/file/file-tree')

program
  .option('-e --exclude <dir>', '排除指定的目录', type.list)
  .parse(process.argv);

fileTree.run(program)
