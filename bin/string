#!/usr/bin/env node

/**
 * Module dependencies.
 */

const program = require('commander')
const package = require('../package.json')
const str = require('../src/string')

program
  .version(package.version)
  .command('prefixsuffix <file> [files...]', 'add prefix and suffix')
  .parse(process.argv)

str.run(program)
